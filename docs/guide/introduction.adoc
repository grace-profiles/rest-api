Grace Profiles encapsulate project commands, templates, and plugins.

To create an application using the `rest-api` profile you can use the next command:

[source, bash]
----
grace create-app com.example.myapp --profile=rest-api
----
